#!/bin/bash

#######################################
u=$HOME/snap/$SNAP_NAME/user2-launch
if [[ -f $u ]]; then
. $u
fi
#######################################

printenv | sort > $SNAP_USER_COMMON/printenv2.sh

exec "$@"
