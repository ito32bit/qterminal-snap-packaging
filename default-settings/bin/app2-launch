#!/bin/bash

#######################################
u=$HOME/snap/$SNAP_NAME/app2-launch-u
if [[ -f $u ]]; then
. $u
fi
#######################################

printenv | sort > $SNAP_USER_COMMON/printenv2.sh

exec "$@"
